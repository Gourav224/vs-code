#include<math.h>

void main()

{	     float a,n,s=0;
	     char ch;
	     clrscr();
	     print("Enter the numbers for calculations :  ");
	     scanf("%f",&n);
	     scanf("%c",&ch);
	     x:
	     if(a!=0)
	     while(n!=0)
	     s=s+n;


	     printf("solution = %.2f",s);
		goto x;
	     getch();
	     getch();


}
