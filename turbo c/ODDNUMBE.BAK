#include<stdlib.h>
#include<time.h>
#define s 10
void main()
{
	int a[s],i;
	clrscr();
	randomize();
  //  srand(time(0));
	printf("Array A is:\n");
	for(i=0;i<s;i++)
	{
		a[i]=random(40);
		if(a[i]%2!=0)
		printf(" %d ",a[i]);

	}
	/*
	for(i=0;i<s;i++)
	{
		a[i];
		printf(" %d ",b[i]);
	}     */
	getch();
}