#include<stdio.h>
void main()
{
	FILE *fp;
	clrscr();
{	char ch;
	fp=fopen("HEY.C","r");
	while(1)
	{
		ch=fgetc(fp);
		if(ch==EOF)
			break;
		printf("%c",ch);
	       //
		getch();
	}
	}
	getch();
}